services:
  - type: web
    name: ganpati-darshan-server
    env: node
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /
    envVars:
      - key: NODE_VERSION
        value: 20.0.0
